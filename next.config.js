/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = nextConfig

module.exports = {
    output: 'export',
    images: {
        formats:['image/webp'],
        unoptimized: true
    },
}
